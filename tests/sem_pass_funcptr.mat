def square(x) { return x * x; }

def apply_twice(f, x) {
    return f(f(x));
}

def main() {
    fp = &square;
    print(fp(5));          // 25

    print(apply_twice(&square, 2));  // square(square(2)) = 16
}
